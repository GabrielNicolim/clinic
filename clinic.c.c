#include <stdio.h>

typedef struct {
    int clientCode;
    char clientName[20];
    int clientAge;
    char clientAddress[40];
    char clientExist;
} typeClient;

void addClient(FILE * clientData) {
    typeClient clientList;
}